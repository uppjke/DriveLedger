<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="1024" height="1024" viewBox="0 0 1024 1024">
  <!-- DL monogram icon source. Export to a 1024×1024 PNG for AppIcon. -->

  <!-- Rounded-square background (iOS will apply its own mask; keep safe margins). -->
  <rect x="96" y="96" width="832" height="832" rx="220" fill="#000000"/>

  <!-- Geometric DL (no fonts) -->
  <g>
    <!-- D (white) built from a stem + bowl; counter is cut out with background color. -->
    <rect x="192" y="288" width="120" height="448" rx="60" fill="#ffffff"/>
    <rect x="252" y="288" width="300" height="448" rx="224" fill="#ffffff"/>
    <rect x="312" y="378" width="180" height="268" rx="134" fill="#000000"/>

    <!-- L (white) -->
    <rect x="570" y="288" width="120" height="448" rx="60" fill="#ffffff"/>
    <rect x="570" y="616" width="262" height="120" rx="60" fill="#ffffff"/>
  </g>
</svg>
